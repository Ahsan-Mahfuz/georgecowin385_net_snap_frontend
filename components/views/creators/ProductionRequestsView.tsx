"use client";

// The "Production Requests" nav item shows the same live production workspace
// (request form + shoot calendar + request queue) as the Production screen.
export { default } from "./ProductionView";
